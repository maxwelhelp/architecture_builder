# Current active source

Current active local file:

- `sequential_matrix_cells_speechcommands_v13_categorized_signal_bus_builder.py`

The v13 hotfix is:

- fix `category_gates_by_stage` construction order inside `SeqAccumulator.summary()`.

If the source file is copied manually from ChatGPT sandbox, place it here and optionally copy it to the project root for running.

Recommended local layout:

- `src/current/sequential_matrix_cells_speechcommands_v13_categorized_signal_bus_builder.py`
- root symlink or copy: `sequential_matrix_cells_speechcommands_v13_categorized_signal_bus_builder.py`
